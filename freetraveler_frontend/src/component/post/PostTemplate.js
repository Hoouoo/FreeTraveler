import React from "react";
import styled from "styled-components";

const PTemplate = styled.div``;

export default function PostTemplate({ children }) {
  return <PTemplate>{children}</PTemplate>;
}
